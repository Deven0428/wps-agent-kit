# WPS 便签自动写入 — 快速上手指南

> 让 AI 帮你自动把内容写进 WPS 新版便签（WPS 笔记），不用手动打开浏览器操作。

---

## 这是什么

一个小工具，装在电脑上后，你对 AI 说"帮我把 XXX 写进 WPS 便签"，AI 就会自动在后台新建一条便签并写好内容，全程你看不到任何弹窗。

**支持的 AI**：Claude Code、Cursor、Codex、Gemini CLI、豆包（Doubao Work）——只要能在你电脑上跑命令行的 AI 都能用。

---

## 你需要准备

1. **一台电脑**（Windows / macOS / Linux 都行）
2. **Node.js**（没装的话去 https://nodejs.org 下载 LTS 版本，一路下一步装好）
3. **一个 WPS 账号**（你平时用的 WPS 账号就行）

---

## 3 步装好

### 第 1 步：拿到代码

从 GitHub 克隆（或直接下载 zip 解压）：

```bash
git clone https://github.com/你的用户名/wps-agent-kit.git
cd wps-agent-kit
```

> 没有 git？直接在 GitHub 点绿色 "Code" 按钮 → "Download ZIP"，解压后进入文件夹。

### 第 2 步：装依赖

```bash
npm install
```

> 第一次会下载 Playwright 浏览器（约 100MB），等它装完。

### 第 3 步：登录 WPS

```bash
node scripts/auth.js
```

会弹出一个浏览器窗口，**手动登录你的 WPS 账号**（扫码或账号密码）。登录成功后浏览器会自动关闭，登录态就保存在你电脑上了。

> ⚠️ 每台电脑都要单独登录一次，登录态不能拷贝。

---

## 装好后怎么用

### 最简单：直接命令行

```bash
node scripts/note-write.js --content "今天下午3点开会，记得带笔记本"
```

写完会提示"写入成功！已验证保存到云端"，打开 WPS 笔记就能看到。

带标题：

```bash
node scripts/note-write.js --title "会议提醒" --content "今天下午3点开会"
```

查看最近的便签：

```bash
node scripts/note-list.js
```

---

## 不同 AI 怎么用

### Claude Code

1. 把项目文件夹放到你的工作目录里
2. 直接对 Claude 说："用 wps-agent-kit 帮我把 XXX 写进 WPS 便签"
3. Claude 会自动找到脚本并执行

> 或者把项目放到 `~/.claude/skills/` 目录下，Claude Code 会自动识别 SKILL.md。

### Cursor

1. 在 Cursor 里打开项目文件夹
2. 按 `Ctrl+J` 打开终端
3. 对 Cursor AI 说："帮我执行 node scripts/note-write.js --content '你要写的内容'"
4. 或者直接在终端里敲命令

### Codex（OpenAI CLI）

1. 在项目目录下启动 codex
2. 说："Run the note-write script with content 'XXX'"
3. Codex 会执行命令并告诉你结果

### 豆包（Doubao Work）

已经装好了 Skill，直接说"帮我把 XXX 写进 WPS 便签"就行，全自动。

### 其他能跑命令行的 AI

通用方法：告诉 AI 项目路径，让它执行：

```bash
cd /path/to/wps-agent-kit
node scripts/note-write.js --content "你要写的内容"
```

---

## 常见问题

### Q: 提示 "未检测到登录会话" 怎么办？
A: 跑一次 `node scripts/auth.js` 重新登录。

### Q: 登录后多久会过期？
A: 一般能维持几周。过期了重新跑 `auth.js` 就行。

### Q: macOS / Linux 上报错找不到 Edge？
A: 用文本编辑器打开 `scripts/note-write.js`，把 `channel: 'msedge'` 改成 `channel: 'chrome'`（需要装了 Chrome），或者删掉这一行让它用默认浏览器。

### Q: 写进去的内容在哪看？
A: 打开 WPS 笔记网页版 https://ainote.kdocs.cn ，或者手机上的 WPS 笔记 App，就能看到。

### Q: 能写富文本（加粗、列表、图片）吗？
A: 当前版本只支持纯文本。富文本需要用 WPS 笔记官方的桌面端 MCP（需要装 WPS 笔记桌面客户端）。

### Q: 这个跟 WPS 笔记官方 MCP 有啥区别？
A: 官方 MCP 功能更强（图片、标签、批量编辑），但必须装 WPS 笔记桌面端并保持运行。这个工具不用装桌面端，轻量，只做"写纯文本便签"这一件事。

---

## 项目里有什么

```
wps-agent-kit/
├── scripts/
│   ├── auth.js           # 登录 WPS（只需跑一次）
│   ├── note-write.js     # 写便签（最常用）
│   ├── note-list.js      # 查看便签列表
│   └── doc-read.js       # 读 WPS 云文档（需要额外配 API Key）
├── SKILL.md              # AI 自动识别用的说明
├── README.md             # 完整技术文档
└── package.json
```

---

## 一句话总结

**clone 下来 → npm install → node scripts/auth.js 登录 → 以后让 AI 跑 node scripts/note-write.js --content "内容" 就行。**
